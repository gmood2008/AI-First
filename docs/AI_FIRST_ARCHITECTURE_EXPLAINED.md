# AI-First 架构解析：ai-first-specs 与 ai-first-runtime 的关系

## 📋 概述

**ai-first-specs** 和 **ai-first-runtime** 是 AI-First 生态系统的两个核心组件，它们采用**关注点分离**的设计原则，实现了**规范与实现分离**的架构模式。

---

## 🔗 两者关系

### 1. **ai-first-specs** - 规范定义层（Specification Layer）

**角色：** "编译器"和"规范仓库"

- **职责：**
  - 定义能力的**接口规范**（YAML 格式）
  - 提供**能力生成工具**（`forge` CLI）
  - 存储**已验证的能力规范**
  - 确保规范的**原子性、确定性和可逆性**

- **核心特性：**
  - 📝 **YAML 规范文件**：定义能力的输入/输出、契约、行为
  - 🤖 **LLM 驱动的生成**：从自然语言生成严格规范
  - ✅ **双重验证**：Schema 验证 + 语义审查
  - 📦 **规范仓库**：集中管理所有能力规范

### 2. **ai-first-runtime** - 执行实现层（Runtime Layer）

**角色：** "执行引擎"和"运行时环境"

- **职责：**
  - **加载** YAML 规范文件
  - **实现** 规范对应的 Handler（处理器）
  - **执行** 能力的具体逻辑
  - **管理** 撤销、审计、安全等运行时特性

- **核心特性：**
  - 🔧 **Handler 实现**：每个能力都有对应的 Python Handler 类
  - 🚀 **运行时引擎**：执行能力、管理状态、处理错误
  - 🔄 **撤销机制**：实现 `undo_strategy` 中定义的撤销逻辑
  - 🔒 **安全沙箱**：路径检查、权限控制、操作确认

---

## 📊 架构关系图

```
┌─────────────────────────────────────────────────────────────┐
│                    ai-first-specs                          │
│                  (规范定义层)                                 │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │  capabilities/validated/stdlib/                    │    │
│  │    ├── io_fs_read_file.yaml  ←── 规范定义          │    │
│  │    ├── io_fs_write_file.yaml                       │    │
│  │    ├── sys_info_get_os.yaml                        │    │
│  │    └── ...                                         │    │
│  └────────────────────────────────────────────────────┘    │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │  forge CLI (规范生成工具)                           │    │
│  │    - forge new "描述"  → 生成 YAML 规范           │    │
│  │    - forge validate    → 验证规范                  │    │
│  └────────────────────────────────────────────────────┘    │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            │ 加载规范
                            │ (通过 AI_FIRST_SPECS_DIR)
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                  ai-first-runtime                           │
│                  (执行实现层)                                │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │  StdLib Loader                                      │    │
│  │    - 读取 YAML 规范                                 │    │
│  │    - 创建 Handler 实例                              │    │
│  │    - 注册到 Registry                                │    │
│  └────────────────────────────────────────────────────┘    │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │  Handler 实现 (src/runtime/stdlib/)                 │    │
│  │    ├── fs_handlers.py  ←── 实现 io.fs.*           │    │
│  │    ├── sys_handlers.py ←── 实现 sys.*              │    │
│  │    └── ...                                            │    │
│  └────────────────────────────────────────────────────┘    │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │  Runtime Engine                                    │    │
│  │    - 执行能力                                       │    │
│  │    - 管理撤销                                       │    │
│  │    - 审计日志                                       │    │
│  │    - 安全控制                                       │    │
│  └────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 工作流程

### 1. **规范定义阶段**（ai-first-specs）

```bash
# 使用 forge CLI 生成新能力规范
forge new "读取文件内容"

# 输出：capabilities/candidates/io_fs_read_file.yaml
```

**生成的 YAML 规范包含：**
- `meta`: 元数据（ID、版本、作者、描述）
- `interface`: 输入/输出接口定义
- `contracts`: 契约（原子性、副作用、确认要求）
- `behavior`: 行为（撤销策略、成本模型、错误处理）

### 2. **规范验证阶段**（ai-first-specs）

```bash
# 验证规范是否符合 AI-First 原则
forge validate capabilities/candidates/io_fs_read_file.yaml

# 验证通过后，移动到 validated/ 目录
```

**验证规则：**
- ✅ **原子性**：一个能力只做一件事
- ✅ **类型严格**：所有输入/输出都有明确类型
- ✅ **可逆性**：必须声明撤销策略
- ✅ **副作用透明**：所有副作用必须声明

### 3. **实现阶段**（ai-first-runtime）

```python
# 在 ai-first-runtime 中实现 Handler
class ReadFileHandler(ActionHandler):
    def execute(self, params, context):
        # 实现读取文件的逻辑
        # 返回符合 YAML 规范定义的输出格式
        pass
```

### 4. **注册阶段**（ai-first-runtime）

```python
# StdLib Loader 自动加载
def load_stdlib(registry, specs_dir):
    # 1. 读取 YAML 规范
    spec = yaml.load("io_fs_read_file.yaml")
    
    # 2. 创建 Handler 实例
    handler = ReadFileHandler(spec)
    
    # 3. 注册到 Registry
    registry.register("io.fs.read_file", handler, spec)
```

### 5. **执行阶段**（ai-first-runtime）

```python
# Runtime Engine 执行能力
result = engine.execute("io.fs.read_file", {
    "path": "/path/to/file.txt",
    "encoding": "utf-8"
}, context)
```

---

## 🚀 能力扩展机制

### ✅ **是的，能力可以不断扩展！**

AI-First 架构设计支持**无限扩展**，扩展流程如下：

### 方式 1：使用 forge CLI 生成新能力（推荐）

```bash
# 1. 进入 ai-first-specs 目录
cd ai-first-specs

# 2. 生成新能力规范
forge new "发送 HTTP DELETE 请求"

# 3. 验证规范
forge validate capabilities/candidates/net_http_delete.yaml

# 4. 规范验证通过后，移动到 validated/ 目录
mv capabilities/candidates/net_http_delete.yaml \
   capabilities/validated/stdlib/
```

### 方式 2：手动创建 YAML 规范

```yaml
# capabilities/validated/stdlib/net_http_delete.yaml
meta:
  id: net.http.delete
  version: 1.0.0
  author: Your Name
  description: Send HTTP DELETE request to a URL

interface:
  inputs:
    url:
      type: string
      description: Target URL
      required: true
    headers:
      type: object
      description: HTTP headers
      required: false
  outputs:
    status_code:
      type: integer
      description: HTTP status code
    response_body:
      type: string
      description: Response body

contracts:
  is_atomic: true
  side_effects: [network_write]
  requires_confirmation: false
  idempotent: true

behavior:
  undo_strategy: no_undo_required_operation_is_idempotent
  cost_model: network_io
```

### 方式 3：实现对应的 Handler

```python
# ai-first-runtime/src/runtime/stdlib/net_handlers.py

class HTTPDeleteHandler(ActionHandler):
    """Handler for net.http.delete"""
    
    def execute(self, params: Dict[str, Any], context: Any) -> ActionOutput:
        url = params["url"]
        headers = params.get("headers", {})
        
        # 实现 DELETE 请求逻辑
        response = requests.delete(url, headers=headers)
        
        return {
            "status_code": response.status_code,
            "response_body": response.text,
            "success": response.ok
        }
```

### 方式 4：注册到 StdLib Loader

```python
# ai-first-runtime/src/runtime/stdlib/loader.py

STDLIB_HANDLERS = {
    # ... 现有能力 ...
    
    # 新增能力
    "net.http.delete": HTTPDeleteHandler,
}
```

### 方式 5：重启运行时

```bash
# 重启 ai-first-runtime MCP 服务器
# Cursor 会自动重新加载所有能力
```

---

## 📈 扩展能力的最佳实践

### 1. **遵循命名规范**

- 使用点分隔的命名空间：`namespace.operation`
- 示例：`io.fs.*`, `sys.info.*`, `net.http.*`, `data.json.*`

### 2. **确保原子性**

- ✅ **好**：`io.fs.read_file` - 只做一件事（读取文件）
- ❌ **坏**：`io.fs.download_and_parse` - 做了两件事

### 3. **定义明确的撤销策略**

```yaml
behavior:
  undo_strategy: delete_file_at_path  # 明确的撤销操作
  # 而不是：undo_strategy: none 或 undo_strategy: n/a
```

### 4. **声明所有副作用**

```yaml
contracts:
  side_effects:
    - filesystem_write  # 明确声明
    - network_read      # 明确声明
```

### 5. **提供完整的类型定义**

```yaml
interface:
  inputs:
    path:
      type: string      # 明确类型
      description: ... # 明确描述
      required: true    # 明确是否必需
```

---

## 🎯 扩展示例：添加新能力

假设我们要添加一个 `cloud.storage.upload` 能力：

### 步骤 1：生成规范

```bash
cd ai-first-specs
forge new "上传文件到云存储"
```

### 步骤 2：实现 Handler

```python
# ai-first-runtime/src/runtime/stdlib/cloud_handlers.py

class StorageUploadHandler(ActionHandler):
    def execute(self, params, context):
        # 实现上传逻辑
        pass
```

### 步骤 3：注册能力

```python
# loader.py
STDLIB_HANDLERS["cloud.storage.upload"] = StorageUploadHandler
```

### 步骤 4：验证

```bash
# 测试新能力
python -c "
from runtime.mcp.server_v2 import create_server
server = create_server()
print('cloud.storage.upload' in server.registry._handlers)
"
```

---

## 🔑 关键优势

### 1. **关注点分离**
- **规范**（specs）与**实现**（runtime）完全分离
- 规范可以独立版本控制和共享
- 实现可以独立优化和测试

### 2. **可扩展性**
- 添加新能力只需：规范 + Handler + 注册
- 无需修改核心引擎代码
- 支持插件化架构

### 3. **类型安全**
- YAML 规范定义严格的类型
- Handler 实现必须符合规范
- 运行时验证确保类型一致性

### 4. **可维护性**
- 规范集中管理，易于查找和更新
- Handler 实现模块化，易于测试
- 清晰的职责边界

---

## 📚 总结

| 组件 | 职责 | 扩展方式 |
|------|------|----------|
| **ai-first-specs** | 规范定义、验证、存储 | 使用 `forge new` 生成新规范 |
| **ai-first-runtime** | 实现、执行、运行时管理 | 实现 Handler 并注册 |

**关系：**
- ai-first-specs 是"规范仓库"，定义"做什么"
- ai-first-runtime 是"执行引擎"，实现"怎么做"
- 两者通过 YAML 规范文件连接

**扩展性：**
- ✅ 能力可以无限扩展
- ✅ 只需添加规范 + Handler + 注册
- ✅ 无需修改核心代码
- ✅ 支持插件化架构

---

**参考文档：**
- [ai-first-specs README](../ai-first-specs/README.md)
- [ai-first-runtime README](../ai-first-runtime-master/README.md)
- [Capability Forge 项目总结](../ai-first-specs/PROJECT_SUMMARY.md)
